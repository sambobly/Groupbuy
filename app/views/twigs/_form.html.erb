<%= form_for(@twig) do |f| %>
  <% if @twig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twig.errors.count, "error") %> prohibited this twig from being saved:</h2>

      <ul>
      <% @twig.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :nest_id %><br>
    <%= f.text_field :nest_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
