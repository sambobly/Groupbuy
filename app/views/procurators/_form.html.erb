<%= form_for(@procurator) do |f| %>
  <% if @procurator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@procurator.errors.count, "error") %> prohibited this procurator from being saved:</h2>

      <ul>
      <% @procurator.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="field">
    <%= f.label :invoice_id %><br>
    <%= f.text_field :invoice_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
