<%= form_for(@concession_type) do |f| %>
  <% if @concession_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concession_type.errors.count, "error") %> prohibited this concession_type from being saved:</h2>

      <ul>
      <% @concession_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :percentage %><br>
    <%= f.number_field :percentage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
