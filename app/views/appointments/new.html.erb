<%= form_for @appointment, url: {action: "create"} do |f| %>

Appointment Name:
	<%= f.text_field :name %>
	Appointment Date:
	<%= f.text_field :date %>

    Time:
    <%= f.text_field :hour %>
	Patient name:
	<%= f.text_field :patient_name %>
    Doctor:

    <%= f.text_field :doctor %>

    <%= f.submit "Create Appointment" %>

<% end %>